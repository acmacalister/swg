apiVersion: v1
kind: Service
metadata:
  name: {{ include "swg.fullname" . }}
  labels:
    {{- include "swg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swg.selectorLabels" . | nindent 4 }}
